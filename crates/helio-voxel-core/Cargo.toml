[package]
name = "helio-voxel-core"
version = "0.1.0"
edition = "2021"
description = "Voxel data structures and GPU types for Helio"

[dependencies]
bytemuck = { workspace = true, features = ["derive"] }
glam = { workspace = true }

[dev-dependencies]
