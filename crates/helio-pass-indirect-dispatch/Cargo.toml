[package]
name = "helio-pass-indirect-dispatch"
version = "0.1.0"
edition = "2021"

[dependencies]
helio-v3 = { workspace = true }
wgpu = { workspace = true, features = ["web", "webgpu", "wgsl"] }
bytemuck = { workspace = true, features = ["derive"] }
