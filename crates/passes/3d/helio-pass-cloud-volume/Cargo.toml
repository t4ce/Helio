[package]
name = "helio-pass-cloud-volume"
version = "0.1.0"
edition = "2021"
description = "Helio-owned authored cloud volume simulation and raymarch pass"
license = "MIT OR Apache-2.0"

[dependencies]
helio-core = { workspace = true }
libhelio = { workspace = true }
wgpu = { workspace = true }
bytemuck = { workspace = true, features = ["derive"] }

[dev-dependencies]
pollster = { workspace = true }
